package bingo_log

import "testing"

// 要完成的功能：
// 传入字符串， 根据不同的方法， 将打印在控制台中
// 并放在


// 测试
func TestLog_SetMode(t *testing.T) {
	// 创建一个对象
	// 设置日志等级
	// 设置对象模式
	// 传入该模式下对应配置
	// 传入日志配置器，用来切割文件等数据
}
